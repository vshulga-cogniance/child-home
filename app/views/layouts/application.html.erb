<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Child UA" %>">

    <title><%= content_for?(:title) ? yield(:title) : "Child UA" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="row">
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main" class="row">
      <%= yield %>
    </main>
    <%= javascript_include_tag "application" %>
  </body>
</html>